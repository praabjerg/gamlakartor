type oldmap = {
  ?place: string option;
  ?title: string option;
  ?uploaded: string option;
  conftype: string;
  thumb_alt: string;
  ?layer: string option;
  ?layers: (string list) option;
}

type maplayer = {
  place: string;
  title: string;
  year: string;
  projectedBy : string;
  uploaded: string;
  sourceName: string;
  sourceLink: string;
  copyright: string;
  minZoom: int;
  maxZoom: int;
  resolution: float;
}
